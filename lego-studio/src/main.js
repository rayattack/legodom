import { Lego } from 'lego-dom';
import './lego-studio.lego';

// Initialize Lego
// We don't need { studio: true } here because we are explicitly using the component in index.html
// and we want to validte the component locally.
Lego.init();
